# 서버 런타임 (Cloudtype 배포 대상) — Python 3.11+
# 규칙: 패키지 추가는 이 파일에 기록 + 단톡 공지 (R2 창구).
# lock 갱신(R2만): 아래 명령으로 재생성 — uvloop은 Windows 미지원이라 제외 마커를 반드시 유지.
#   pip freeze | sed 's/^uvloop==\(.*\)/uvloop==\1 ; sys_platform != "win32"/' > requirements.lock.txt
# 이후 전원은 lock으로 설치.
fastapi~=0.115.0
uvicorn[standard]~=0.32.0
sqlalchemy~=2.0.35
psycopg2-binary~=2.9.9
pydantic~=2.9.0
anthropic~=0.39.0
python-dotenv~=1.0.1
httpx~=0.27.0
