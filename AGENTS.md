# 봄내마실 — AI 에이전트 공통 지침

이 저장소에서 일하는 모든 AI 코딩 도구(Codex, Claude Code 등)의 공통 규칙이다. 상세한 설계·결정은 docs/에 있고, 이 파일은 "어기면 안 되는 것"만 담는다.

## 먼저 읽을 문서 (작업 영역에 해당하는 것)

- `docs/00-overview.md` — 무엇을 만드는가 (제품 정의의 진실, 전원)
- `docs/20-team.md` — 역할·브랜치·이슈 규칙 (전원)
- `docs/10-architecture.md` — 시스템 설계·확정 기술 결정 (개발 전원)
- `docs/25-screens.md` — 화면 동작의 진실 (frontend 작업 시 필수)
- `docs/15-data.md` — 데이터 목록·수집 규칙 (pipeline 작업 시 필수)
- `docs/30-api-contract.md` — API 계약 (동결 문서 — 아래 규칙 3 참조)

## 절대 규칙

1. **폴더 = 주인.** 자기 역할의 폴더만 수정한다: `frontend/`=R1(우영찬) / `backend/`=R2(김태우) / `backend/app/services/scoring/`+`pipeline/`=R3(최우혁) / `backend/app/services/quest_builder/`+`llm/`=R4(최서준). 남의 폴더를 고쳐야 할 것 같으면 코드를 고치지 말고 해당 이슈에 코멘트를 남겨라
2. **import는 한 방향**: routers → quest_builder → scoring. `services/` 하위는 FastAPI를 import하지 않는다. 남의 패키지는 공개 함수만 사용
3. **계약 4종은 동결** (API 명세 `30-api-contract.md`·점수 함수·GeoJSON 형식·build_quests). 필드 추가·변경·이름 바꾸기 금지 — 필요하면 구현하지 말고 사람에게 보고하라
4. **main 직접 push 금지.** 브랜치 `feat/<이슈번호>-<역할>-<요약>` → PR 제목 `[역할] 요약 (#이슈번호)` → 태우 승인 후 squash 머지. PR 본문에 `Closes #번호`
5. **비밀키를 코드·커밋에 절대 넣지 않는다** — `.env`만 사용. 이 레포는 공개다
6. **현재 시각을 직접 읽지 않는다** — `now()` 금지, 기준 시각 유틸(`DEMO_NOW` 환경변수 지원) 하나만 사용
7. **버스 경로·무환승·소요시간은 접근성 점수표 조회만** — 어디서도 재계산하지 않는다
8. **커밋·PR에 AI 도구 서명 금지** (Co-authored-by 등) — 대회 산출물은 팀 명의다

## 확정된 결정 (다르게 구현하거나 "개선"을 제안하지 마라)

- 포인트: 가게 인증 +40 / 기록 저장 +40(문답 1개 이상 응답 시) / 완주 +20 = 100. 잔액은 별도 API 없이 세션·인증·기록 응답의 `balance` 필드
- TAGO 실시간 미사용 — 버스 안내는 "약 N분 간격 운행" 고정 표기
- 퀘스트 상태: recommended → started → stamped → recorded. 동시 진행 1개
- 출발지 2단 선택(행정동 필수 + 정류장 선택), 시간은 시작~종료 창(최소 60분), 예산은 5구간 버튼
- 인증: QR·코드 = 가게별 4자리 코드 대조(HMAC 없음), 재인증은 멱등(성공 응답), 영수증 사진은 저장하지 않음
- 로그인 없음(익명 세션) / GPS 없음 / 만 14세 게이트 / 삭제는 연쇄 삭제

## 애매하면

- 화면 동작이 문서에 없다 → 임의로 정하지 말고 이슈에 질문 코멘트를 남기고, 그 부분을 비워둔 채 다른 작업을 진행하라. 기획 공백의 결정 권한은 사람에게 있다 (`docs/20-team.md` 5-1)
- 이 파일과 docs/가 충돌하면 → 이 파일이 우선. 발견 즉시 사람에게 보고하라
