# design/ — 시각 참고 자료

`prototype.html` — 봄내마실 프로토타입 v2 (단독 실행, 브라우저로 열면 동작). **시민 화면 5종의 시각·레이아웃 참고용**이다.

## 위계 (중요)
**동작의 진실은 `docs/25-screens.md`다.** 프로토타입과 정의서가 다르면 정의서가 우선한다.

## 알려진 편차 (구현 시 프로토타입을 따라하면 안 되는 것)
1. **실시간 도착 표기** (`지금 N분 후 도착`, busMode 기본 '실시간') — 폐기된 기능. 구현은 "약 N분 간격 운행" 고정 표기만 (10-architecture 6-1)
2. **대시보드 화면 없음** — 화면 6번(#33)은 이 프로토타입 범위 밖. 별도 구현
